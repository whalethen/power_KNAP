const assert = require('assert');
const mocha = require('mocha')

describe('Testing Intensifies', function() {
  describe('#indexOf()', function() {
    it('should return -1 when blah blah blah', function() {
      assert.equal(-1, [1, 2, 3].indexOf(4));
    })
  })
})