const assert = require('assert');
// const mocha = require('mocha');

describe('Testing Intensifies', () => {
  describe('#indexOf()', () => {
    it('should return -1 when blah blah blah', () => {
      assert.equal(-1, [1, 2, 3].indexOf(4));
    });
  });
});
